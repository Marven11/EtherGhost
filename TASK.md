# 环境

这里是EtherGhost的源码，EtherGhost是一个webshell管理器，支持PHP Linux Shell JSP等webshell

EtherGhost在启动后会对外提供API，并且尝试启动用户的浏览器管理

EtherGhost使用poetry管理代码，使用pyright保证代码质量

这个项目没有在MacOS上测试过，需要注意兼容性

# 任务

- [ ] 使用浏览器MCP探索当前主页功能，报告当前session-icon-others菜单有什么功能，截图并将截图移动到/tmp规划文件夹
- [ ] 使用浏览器MCP随便选择一个webshell打开文件管理功能，截图并将截图移动到/tmp规划文件夹
- [ ] 询问视觉大模型两个截图中的内容，验证视觉大模型可以使用。必须完成此任务！
- [ ] 我们需要完成一个功能：批量执行命令
- [ ] 完成“批量执行命令”功能的前端页面
  - 最下方有一个文本框输入命令，还有“执行”按钮和“导出”按钮，均用合适的图标表示
  - 上方有一个可以滚动的主区域，分成多行展示每个webshell以及命令的执行结果
  - 每行左边展示每个webshell的名字和类型，右边展示命令的执行结果
  - 每行的左上角有一个小圆点，颜色可为红色、黄色闪烁或者绿色，用来展示是否正在执行（黄闪）以及执行是否成功（红绿）
    - 执行命令前不需要展示状态，此时小圆点不显示
  - 在对接后端之前先完成前端，填充示例数据并尝试滚动
  - 在实际添加这个功能之前先完成页面，可以先不完成“执行”和“导出”功能，但是需要展示小圆点，因为需要截图给视觉大模型看
  - 截图询问视觉大模型
    - 页面风格是否和主页截图和文件管理页面截图一致
    - 按钮、卡片、图标风格是否一致
    - 以十分制给页面美观度打分（假设主页美观度为8.5）并给出理由
    - 如果不美观则详细描述问题并提供几个建议
  - 修改界面直到视觉大模型给出8分以上的分数为止
- [ ] 完成“批量执行命令”功能的功能对接
  - 实际添加功能到主页：在多选模式右键菜单中添加“批量执行命令”的功能
  - 完成执行功能：点击后并发调用API控制webshell执行命令并展示结果，同时控制小圆点展示状态
  - 完成导出功能：点击导出后导出一个json，包含每个webshell的id, 名字，执行是否成功，以及执行结果等信息
- [ ] 打开浏览器MCP，测试以上功能，截图并询问视觉大模型是否存在相关组件
  - 测试执行功能是否可以正常使用
  - 测试导出功能是否可以正确导出json

# 暂时搁置

# 注意

- 使用nix develop --command poetry run python -m ether_ghost --no-browser启动API服务器，不要自动带起浏览器
- 测试前端时，先在一个终端中启动API服务器，再进入frontend/在另一个终端启动npm run dev
- 缺少public/时: 使用nix develop --command poetry run build-frontend构建前端
- 需要在浏览器中右键元素时：使用event而非.click函数
- 使用浏览器时避免使用verbose查看html

